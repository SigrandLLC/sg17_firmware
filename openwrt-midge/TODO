* make list all of base-files* installed to root fs.
* replace ntpclient by openntpd
* rm crontab, cron, /etc/init.d/timesync
* убрать всё лишнее, архитектуры, что не используется в построении образов sigrand.
* Add '-nofw' to kernel extraversion


* Translate:
1-2014
	* DSLAM:
		* added statistics reset for dsl ifaces and switches ports
		* added uplinks configuration (duplex, speed, flow control)
		* added trunk config
		* added option to on/off dsl ifaces
		* addes time counters (uptime_all, downtime_all)
		* fixed messages about state of dsl ifaces


.PHONY: choice_config
choice_config: $(CONFIG)/mconf
	@rm -f .config
	@touch .config
	@$(CONFIG)/mconf ConfigChoice.in
	@mv .config

ConfigChoice.in:
#

mainmenu "Config file choice"

#config_admboot
#config_sigrand
#config_sviazinvest
#config_zelax

config CONFIG_CHOICE

choice
	prompt "Config name"
	default CONFIG_CHOICE_SIGRAND
	help
	  Select the config/OEM name you wish to use.

	config CONFIG_CHOICE_ADMBOOT
		bool "admboot"

	config CONFIG_CHOICE_SIGRAND
		bool "sigrand"

	config CONFIG_CHOICE_SVIAZINVEST
		bool "sviazinvest"

	config CONFIG_CHOICE_ZELAX
		bool "zelax"

endchoice

